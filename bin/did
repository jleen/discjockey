#!/usr/bin/env zsh

[[ -f ~/.dj ]] && . ~/.dj

CDROM=
if [[ `uname` == 'Darwin' ]]; then
    DWAIT=${DJ_WAIT:-${0%/*}/dwait}
    CDROM=${DJ_CDROM:-/`drutil status|grep Name|cut -d/ -f2-`}
    $DWAIT > /dev/null
fi

cd-discid $CDROM
